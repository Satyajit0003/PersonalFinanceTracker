server:
  port: 8084

spring:
  application:
    name: CATEGORY-SERVICE
  data:
    mongodb:
      host: localhost
      port: 27017
      database: financeDb
  config:
    import: configserver:http://localhost:8086




